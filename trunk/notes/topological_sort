1. get all nodes that have no parent nodes that are in the nodes list
1. a) if no such node is found, but there are still nodes in the nodes list, the graph is cyclic
2. add them to the sorted list
3. delete them from the nodes list